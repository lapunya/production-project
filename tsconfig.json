{
  "compilerOptions": {
    // для абсолютного импорта
    // "baseUrl": ".",
    "outDir": "./dist/",

    // не позволяет использовать переменные без явного указания типа
    "noImplicitAny": true,

    // указываем модульную систему (различные способы модульной сборки)
    "module": "esnext",

    // тут указываем версию, в которую код будет компилироваться, для поддержки большинства браузеров
    "target": "es5",
    "jsx": "react-jsx",

    // позволяет компилятору обрабатывать не только ts файлы, но и js
    "allowJs": true,
    "moduleResolution": "node",

    // позволяет использовать простой import ... from ... даже если у пакета нет дефолтного экспорта внутри
    "allowSyntheticDefaultImports": true,

    // позволяет работать с пакетами, которые используют common js, как с обычными с помощью import
    "esModuleInterop": true
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}